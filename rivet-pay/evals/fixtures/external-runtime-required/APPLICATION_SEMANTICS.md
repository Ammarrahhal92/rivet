# Application semantics

Source contains environment-dependent verification and routing. Whether production actually reuses the secret is a deployment fact unavailable to source-only review, so the candidate remains unresolved rather than confirmed.

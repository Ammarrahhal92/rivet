# Application semantics

A is current, B legitimately supersedes A, and B becomes terminal. The identity mismatch guard applies only while the current authority is active. A later distinct valid event for A is accepted after terminality and restores paid value. A genuinely new identity is a separate lineage branch.

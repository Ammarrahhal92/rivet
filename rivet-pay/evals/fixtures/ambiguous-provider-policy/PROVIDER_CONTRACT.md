# External contract

MockPay documents an external state named `delinquent_final`. The contract establishes that the state can exist, but does not define the reviewed product's local entitlement policy.

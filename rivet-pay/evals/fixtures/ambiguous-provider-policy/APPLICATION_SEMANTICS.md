# Application semantics

One entitlement predicate denies the state when persisted, while the event interpreter can fall back to a previous active state. The policy dependency remains unresolved and must not be invented from the external state alone.

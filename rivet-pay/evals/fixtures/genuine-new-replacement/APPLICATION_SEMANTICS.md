# Application semantics

The application records a new authority creation transition for C and binds the resulting entitlement to C. It does not accept a historical prior object as a substitute for the new transition.

# Application semantics

Refund writes revoked state and retains grant identity. A later newer same-authority generic update writes active, and the entitlement function returns paid value. The mutation path is deterministic and no runtime fixture is required to establish the violation.
